# Project Rules

- **Service IDs**: Whenever using an ID for a service (API calls, data fetching, routing), prioritize using the numeric ID (e.g., `courseId`) instead of the standard string `id`, as expected by the backend.

